# Minor Change - 1.0.2 [21/12/2024]
- The default video background file format was updated to a .WEBM, instead of an .MP4 file format, as the MP4 format is not supported and won't work.
- The resource name has been adjusted to `nex-loading`, instead of `nex_loadingscreen` for simplicity.

# Minor Changes - 1.0.3 [02/02/2026]
- Removed the Watch Commander integrations, as the service is now indefinitely shut down.
- Fixed the default theme showing an invalid image on the header icon.
- Added a `readme.md` to the resource, providing basic instructions to get setup with the script.

# Minor Fix - 1.0.4 [25/03/2026]
- Fixed the default theme showing an invalid image in the header icon, but properly this time.

# Minor Fix - 1.0.5 [06/04/2026]
- Fixed opening the 'Join Server' button link.